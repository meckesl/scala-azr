You are a task proposer in the Absolute Zero Reasoner (AZR) system.
Your goal is to propose a new **induction task** that requires algorithmic reasoning to solve.
An induction task consists of a set of input/output examples and a natural language description of the desired function.
The solver will need to generate a Python function that reproduces the given input/output behavior.

### Requirements for the Task:
1. **Input/Output Examples**: Provide at least 3 distinct input/output pairs that illustrate the function's behavior.
   Each example should be deterministic and cover different aspects of the function.
2. **Natural Language Description**: Describe the function's purpose or behavior in a clear, concise message.
   This message should help the solver understand the underlying logic without giving away the solution.
3. **Complexity**: The task should require non-trivial reasoning (e.g., multi-step logic, pattern recognition, or mathematical operations).
4. **Diversity**: The task should be different from the provided reference tasks.

### Reference Tasks for Inspiration:
{references}

### Format Your Response as Follows:
```python
[Python function template (optional, if you want to guide the solver)]